<Page x:Class="Bolnica.Forms.FormIstorijaOcenaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bolnica.Forms"
      mc:Ignorable="d" 
      d:DesignHeight="390" d:DesignWidth="1000"
      Title="FormIstorijaOcenaPage">

    <Grid>
        <DataGrid Name="oceneGrid" ItemsSource="{Binding PrikazNezavrsenihPregleda}" HorizontalAlignment="Center" VerticalAlignment="Top" 
                  Height="350" Width="950" AutoGenerateColumns="False" SelectionUnit="FullRow" IsReadOnly="True">
            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="Background" Value="#FF00DFFF"></Setter>
                    <Setter Property="FontWeight" Value="SemiBold"></Setter>
                    <Setter Property="BorderThickness" Value="0,0,1,2"></Setter>
                    <Setter Property="BorderBrush" Value="black"></Setter>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding Datum}"  Width="150"></DataGridTextColumn>
                <DataGridTextColumn Header="Primalac" Binding="{Binding Primalac}" Width="200"></DataGridTextColumn>
                <DataGridTextColumn Header="Ocena" Binding="{Binding BrojOcene}" Width="50"></DataGridTextColumn>
                <DataGridTextColumn Header="Komentar" Binding="{Binding Sadrzaj, FallbackValue='-'}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
